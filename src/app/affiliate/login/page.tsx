import {redirect} from "next/navigation";import {getAffiliateUser} from "@/lib/supabase/auth-server";import {loginAffiliate} from "../actions";import "../affiliate.css";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){if(await getAffiliateUser())redirect("/affiliate/dashboard");const {error}=await searchParams;return <main className="affiliate-shell"><section className="affiliate-card login"><h1>PADOX <span>PRO</span></h1><p>LOGIN AFFILIATE</p>{error?<div className="alert">Email, kata laluan atau akses tidak sah.</div>:null}<form action={loginAffiliate}><label>Email<input name="email" type="email" required/></label><label>Kata laluan<input name="password" type="password" required/></label><button>Log masuk</button></form><small>Password hanya boleh ditetapkan oleh admin.</small></section></main>}
